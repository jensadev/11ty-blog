{% if navigation.items %}
    <nav class="container">
        <ul>
            {% for item in navigation.items %}
                <li>
                    <a href="{{ item.url }}">{{ item.text }}</a>
                </li>
            {% endfor %}
        </ul>
    </nav>
{% endif %}